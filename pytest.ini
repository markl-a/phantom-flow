[pytest]
# Engine tests only — the vendored subtrees (ai_automation_framework/,
# data_analysis/) ship their own suites and are not part of the phantom-flow
# engine contract, so they are excluded here.
testpaths = tests
pythonpath = .
addopts = -ra
